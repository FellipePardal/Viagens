{
  "name": "viagens",
  "version": "0.1.0",
  "private": true,
  "description": "Passageiro — planejador de viagens colaborativo",
  "scripts": {
    "build": "node build.js",
    "dev": "node serve.js",
    "start": "node serve.js",
    "db:setup": "node scripts/run-sql.js supabase/schema.sql supabase/policies.sql supabase/realtime.sql",
    "db:sql": "node scripts/run-sql.js"
  },
  "dependencies": {
    "@babel/standalone": "^7.24.0"
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "pg": "^8.23.0"
  }
}
